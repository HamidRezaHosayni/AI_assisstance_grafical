
## 🧠 برنامه‌ی شما چیست؟

شما یک **دستیار هوش مصنوعی تعاملی محلی** ساخته‌اید که:

- **از طریق رابط گرافیکی (PyQt6)** با کاربر ارتباط برقرار می‌کند  
- **پیام‌های کاربر را تحلیل می‌کند** تا تشخیص دهد آیا نیاز به انجام یک کار عملیاتی دارد یا فقط پاسخ متنی کافی است  
- در صورت نیاز به انجام کار عملیاتی (مثل ساخت فایل، اجرای دستور، جستجو)، **یک ابزار (tool) را اجرا می‌کند**  
- **تمام ابزارها به‌صورت ماژولار و جداگانه** نوشته می‌شوند و سیستم به‌صورت خودکار آن‌ها را مدیریت می‌کند  
- از **مدل‌های محلی** (مثل Ollama) برای پردازش زبان استفاده می‌کند  
- **پشتیبانی کامل از زبان فارسی** دارد (هم در ورودی و هم در خروجی)  
- از **اجرای ناامن جلوگیری می‌کند** — هر ابزار در محیطی محدود و بدون دسترسی آزاد به سیستم اجرا می‌شود  

---

## 🔧 چگونه باید برای این برنامه ابزار نوشت؟

هر ابزار در این سیستم **یک واحد مستقل و استاندارد** است. برای ساخت یک ابزار جدید، باید مراحل زیر را دنبال کنید:

### 1. ساختار پوشه
در پوشه‌ی اصلی پروژه، مسیر زیر را ایجاد کنید:
```
project/
└── tools/
    └── نام_ابزار_شما/       ← مثلاً: file_creator یا system_info
        └── main.py          ← فایل اجراشونده‌ی اصلی
```

> نام ابزار باید **انگلیسی، بدون فاصله و با استفاده از زیرخط (snake_case)** باشد.

---

### 2. فایل `main.py` — واسط استاندارد

این فایل **پل ارتباطی بین سیستم اصلی و ابزار شما** است. باید دقیقاً این قوانین را رعایت کند:

#### ✅ ورودی:
- از **`stdin`** (ورودی استاندارد) یک شیء JSON دریافت می‌کند  
- ساختار ورودی دقیقاً همانی است که در `tools.json` تعریف کرده‌اید  

#### ✅ خروجی:
- فقط و فقط یک **شیء JSON معتبر** را در `stdout` چاپ می‌کند  
- ساختار خروجی **حتماً** باید شامل این فیلدها باشد:
  ```json
  {
    "status": "success" یا "error",
    "message": "یک پیام توضیحی به زبان فارسی"
  }
  ```
- **هیچ چیز دیگری** (مثل `print("Loading...")` یا خطاهای لاگ) نباید در `stdout` چاپ شود  
- اگر خطا رخ داد، کد خروجی (`exit code`) باید غیر از صفر باشد

#### ✅ مثال ساده:
```python
import sys
import json

def create_file(filename: str, content: str):
    with open(filename, "w") as f:
        f.write(content)
    return {"status": "success", "message": f"فایل '{filename}' ساخته شد."}

if __name__ == "__main__":
    try:
        input_data = json.load(sys.stdin)
        filename = input_data["filename"]
        content = input_data["content"]
        result = create_file(filename, content)
        print(json.dumps(result, ensure_ascii=False))
    except Exception as e:
        print(json.dumps({
            "status": "error",
            "message": f"خطا: {str(e)}"
        }, ensure_ascii=False))
        sys.exit(1)
```

---

### 3. تعریف ابزار در `tools.json`

در ریشه‌ی پروژه، فایلی به نام `tools.json` وجود دارد که لیست تمام ابزارها را نگه می‌دارد.  
برای ابزار جدیدت، یک ورودی شبیه به این اضافه کن:

```json
{
  "name": "file_creator",
  "description": "ایجاد فایل جدید با محتوای مشخص",
  "parameters": {
    "type": "object",
    "properties": {
      "filename": {"type": "string"},
      "content": {"type": "string"}
    },
    "required": ["filename", "content"]
  }
}
```

- `name`: باید دقیقاً همان نام پوشه‌ی ابزار باشد  
- `description`: توضیحی کوتاه برای کمک به سیستم در انتخاب ابزار  
- `parameters`: ساختار ورودی‌ای که ابزار انتظار دارد (بر اساس استاندارد JSON Schema)

---

### 4. قوانین طلایی برای نوشتن ابزار

- ❌ هیچ‌گاه از `input()` یا `print()` برای تعامل با کاربر استفاده نکن — فقط `stdin`/`stdout`  
- ✅ تمام خطاها را در JSON خروجی گزارش کن  
- ✅ از مسیرهای مطلق سیستمی استفاده نکن — فقط روی دسکتاپ یا مسیرهای نسبی کار کن  
- ✅ در صورت نیاز به اینترنت یا دسترسی خاص، ابتدا از کاربر تأیید بگیر (در لایه UI)  
- ✅ فایل‌های موقت را پس از اجرا حذف کن

---

## ✅ جمع‌بندی

برنامه‌ی شما یک **چارچوب اجرای ابزارهای هوشمند** است که:  
- ورودی کاربر را تحلیل می‌کند  
- ابزار مناسب را انتخاب و اجرا می‌کند  
- خروجی را به کاربر نمایش می‌دهد  

و هر ابزار جدید باید:  
1. یک پوشه با ساختار استاندارد داشته باشد  
2. یک `main.py` با ورودی/خروجی JSON داشته باشد  
3. در `tools.json` تعریف شده باشد  

این طراحی باعث می‌شود افزودن هزاران ابزار جدید **کاملاً ایمن، ساده و بدون تغییر در کد اصلی** امکان‌پذیر باشد.


=================================================================================================================

